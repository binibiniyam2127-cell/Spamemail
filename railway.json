{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash setup.sh"
  },
  "deploy": {
    "startCommand": "uvicorn deployment.api.app_fastapi:app --host 0.0.0.0 --port 8000",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
